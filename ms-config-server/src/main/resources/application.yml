server:
  port: 7070

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jhanrq8888/DAD2024-1.git
          searchPaths: config-data
          default-label: main
